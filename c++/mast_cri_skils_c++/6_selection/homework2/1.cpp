#include <iostream>
using namespace std;

int main()
{
    int a, b, c, d, e, f, g, h, i, j;
    cin >> a >> b >> c >> d >> e >> f >> g >> h >> i >> j;

    int answer {0};

    if (a > answer)
        answer = a;
    if (b > answer)
        answer = b;
    if (c > answer)
        answer = c;
    if (d > answer)
        answer = d;
    if (e > answer)
        answer = e;
    if (f > answer)
        answer = f;
    if (g > answer)
        answer = g;
    if (h > answer)
        answer = h;
    if (i > answer)
        answer = i;
    if (j > answer)
        answer = j;
    
    cout << answer << "\n";

    return 0;

}
