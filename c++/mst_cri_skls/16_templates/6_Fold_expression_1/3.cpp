/*
   Supported 32 operators

   + - * / % ^
   & | = ^= &= |=
   <> == != <= >=
   << >>
   += -= *= /= %=
   <<= >>=
   && || , .* ->*


   In binary fold, both ops must be the same
        CE: return (args * ... + 1);
*/
